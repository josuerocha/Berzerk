#include "Colisao.h"
#include "Estruturas.h"

Colisao::Colisao()
{
}


Colisao::~Colisao()
{
}

bool Colisao::Checa(Dimensao a, Dimensao b){
/*	int left1, left2;
	int right1, right2;
	int top1, top2;
	int bottom1, bottom2;

	left1 = a.c.x;
	left2 = b.c.x;
	right1 = a.c.x + a.largura;
	right2 = b.c.x + b.largura;
	top1 = a.c.y;
	top2 = b.c.y;
	bottom1 = a.c.y + a.altura;
	bottom2 = b.c.y + b.largura;

	if (bottom1 < top2) return(false);
	if (top1 > bottom2) return(false);

	if (right1 < left2) return(false);
	if (left1 > right2) return(false);*/
	return false;
}